<ul class='country-list'>
  {{#each []}}
    <li class="country-list">
      <img src='{{flags.svg}}' alt='{{name.official}}' class="flag" /> <p class="countryName">{{name.official}}</p>
    </li>
  {{/each}}
</ul>